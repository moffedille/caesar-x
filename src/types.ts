export enum ViewType {
    Data = 'data',
    Map = 'map',
};

export type Entry = {
    seq: string;
    name_first: string;
    name_last: string;
    age: string;
    street: string;
    city: string;
    state: string;
    latitude: string;
    longitude: string;
    ccnumber: string;
};